# Diamond pricing example

This example demonstrates an exploratory analysis in R on ggplot2's built-in `diamonds` dataset.

See `dockerize.py` for how to use `tugboat-cli` to programmatically package the pipeline into a Docker image.
