# California housing example

This example demonstrates an exploratory analysis in Python on sklearn's California housing dataset.

See `dockerize.py` for how to use `tugboat-cli` to programmatically package the pipeline into a Docker image.
